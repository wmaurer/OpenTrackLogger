<UserControl x:Class="OpenTrackLogger.Views.UploadTrackView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="480" d:DesignWidth="480">
    
    <Grid x:Name="LayoutRoot" Background="{StaticResource PhoneChromeBrush}">
        <StackPanel Orientation="Vertical">
            <TextBlock Text="Exporting to GPX:" Margin="0,20,0,10"/>
            <ProgressBar x:Name="ExportProgressBar" IsIndeterminate="False" Maximum="100"/>
            <TextBlock x:Name="ExportProgressText"/>
            <TextBlock Text="Creating zip file:" Margin="0,20,0,10"/>
            <ProgressBar x:Name="ZipProgressBar" IsIndeterminate="False" Maximum="100"/>
            <TextBlock x:Name="ZipProgressText"/>
            <TextBlock Text="Uploading to OneDrive:" Margin="0,20,0,10"/>
            <ProgressBar x:Name="UploadProgressBar" IsIndeterminate="False" Maximum="100"/>
            <TextBlock x:Name="UploadProgressText"/>
        </StackPanel>
    </Grid>
</UserControl>